1) Le commit create in una repository locale non vengono sincronizzate
automaticamente con la repository online collegata.
2)Il nuovo nome standard del branch principale utilizzato come
convenzione in GIT è master.
3)Come descriveresti il workflow dei comandi di GIT?
    git add -> git commit -> git push
4)Il comando da utilizzare per vedere l’elenco di tutte le commit del
branch in cui ci si trova e i loro hashcode è: git log

5)Il comando per effettuare il rollback ad una commit precedente è: //rispondo dopo

6) Scrivi due motivazioni per giustificare l’utilizzo di GIT:
    1) è molto comodo per il lavorod i squadra in quanto si può lavorare sullo stesso progetto      senza intralciarsi seguendo due branch differenti
     2) è molto comodo in caso si stia modificando un progetto e quest'ultimo non funzioni più      perchè grazie a GIT si può tornare allo stato funzionante del progetto che rimane salvato.